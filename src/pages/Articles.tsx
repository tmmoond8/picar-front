/** @jsx jsx */
import { jsx } from '@emotion/core';

export default (function Products(): JSX.Element {
  // const {
  //   product: { products: allProducts, toggleWish, wishs },
  //   ui: { productPage, productPageUp, productScroll },
  // } = useStore();

  return <p>article pages</p>;
});
